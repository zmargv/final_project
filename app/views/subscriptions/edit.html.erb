<!-- Validation failure messages -->
<% if @subscription.errors.any? %>
  <% @subscription.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Subscription #<%= @subscription.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_subscription/<%= @subscription.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for user_id -->
      <div class="form-group">
        <label for="user_id" class="control-label">
          User
        </label>

        <input type="text" id="user_id" name="user_id" placeholder="User" class="form-control" value="<%= @subscription.user_id %>">
      </div>

      <!-- Label and input for topic_id -->
      <div class="form-group">
        <label for="topic_id" class="control-label">
          Topic
        </label>

        <input type="text" id="topic_id" name="topic_id" placeholder="Topic" class="form-control" value="<%= @subscription.topic_id %>">
      </div>

      <button class="btn btn-warning">
        Update Subscription
      </button>
      or
      <a href="/subscriptions/<%= @subscription.id %>">Cancel</a>
    </form>
  </div>
</div>
